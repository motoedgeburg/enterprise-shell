/**
 * Centralized route path constants.
 *
 * Use these instead of hardcoded strings so that a path change only requires
 * updating one place.
 */
export const ROUTES = {
  LOGIN: '/login',
  LOGIN_CALLBACK: '/login/callback',
  DASHBOARD: '/dashboard',
  SEARCH: '/search',
  RESULTS: '/results',
  RECORDS_NEW: '/records/new',
  RECORD_DETAIL: '/records/:id',
};
